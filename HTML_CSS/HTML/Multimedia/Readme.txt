Tres secciones con etiqueta <section>
- 1ª una imagen de la carpeta img
- 2ª un video de la carpeta video. Redimensionado, muestra controles y en bucle. Para que se reproduzca automaticamente tiene que tener el atributo muted
     (sin audio)
- 3ª un audio de la carpeta audio. Muestra controles y en bucle. Para que se reproduzca automaticamente tiene que tener el atributo muted (sin audio)
